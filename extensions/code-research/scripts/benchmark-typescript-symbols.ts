// TypeScript source for the executable benchmark reporter in benchmark-typescript-symbols.js.
// The JavaScript entrypoint is kept because the extension is intentionally buildless.
import './benchmark-typescript-symbols.js';
